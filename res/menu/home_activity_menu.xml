<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:avisreader="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_action_action_search"
        android:title="@string/search"
        avisreader:showAsAction="always|collapseActionView"
        avisreader:actionViewClass="android.support.v7.widget.SearchView"/>

    <item
        android:id="@+id/action_add"
        android:icon="@drawable/ic_action_content_new"
        android:title="@string/add"
        avisreader:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_settings"
        android:title="@string/settings"
        avisreader:showAsAction="never"/>
</menu>